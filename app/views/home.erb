
<% if (slides = Slide.where(:series.in => [nil, 'signed-in'])).count > 0 %>
  <%= partial :carousel, :locals => {:slides => slides} %>
<% end %>

<script>
  $(function () {
    $('#map').load('/map?<%= {form: true, accounts: !Config['HIDE_PEOPLE_BY_DEFAULT'], events: Config['SHOW_EVENTS_BY_DEFAULT'], organisations: Config['SHOW_ORGS_BY_DEFAULT']}.to_query %>')
  });
</script>
<div id="map"></div>
